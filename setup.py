from setuptools import setup

setup(name='geodmaip',
      version='0.1',
      description='geolocate IP addresses with DMA information',
      url='http://github.com/rtphokie/geodmaip',
      author='Tony Rice',
      author_email='rtphokie@gmail.com',
      license='MIT',
      packages=['geodmaip'],
      zip_safe=False)
